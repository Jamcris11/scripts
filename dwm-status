#!/usr/bin/env bash

RAM=$(free -h | grep Mem: | awk '{ print $3 }')
TIME=$(date '+%I:%M %p')
DATE=$(date '+%b %e')
HDD=$(df -h | grep sda3 | awk '{print $4}')

dwm -s " $HDD | $RAM | $DATE | $TIME "
